apply plugin: 'com.android.application'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.3"

    defaultConfig {
       // applicationId "com.mbs.sdk.ads.demo"
        applicationId "mobi.myclean.boost"
        minSdkVersion 14
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }

    lintOptions {
        abortOnError false
    }

    signingConfigs {
        release {
            storeFile file('ads-demo.keystore')
            storePassword 'ads_demo'
            keyAlias 'ads-demo-key'
            keyPassword 'ads_demo'
        }
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }
}

dependencies {
    compile 'com.android.support:support-v4:23.+'

    // Ms Ads Sdk
    compile 'com.mbs.sdk.rich.ads:msads:1.5.2.411'

    testCompile 'junit:junit:4.12'
}
